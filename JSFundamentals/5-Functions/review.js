function f(parameter1, parameter2) {
    return "something";
}

f("argument1", "argument2");

function greeting(fistName, lastName) {
    console.log(firstName + " " + lastName);
}

greeting("Smith", "John"); //returns names in backwards order